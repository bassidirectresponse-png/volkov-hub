"use client";
import { BarChart, Bar, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

export function DashboardChart({ data }: { data: { day: string; receita: number; gastos: number; lucro: number }[] }) { if (!data.length) return <div className="grid h-[275px] place-items-center text-center"><p className="max-w-xs text-xs leading-5 text-[var(--muted)]">A curva de receita, gastos e lucro aparecerá quando houver vendas ou gastos importados.</p></div>; return <div className="h-[275px]"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} barGap={3}><CartesianGrid vertical={false} stroke="rgba(255,255,255,.08)"/><XAxis dataKey="day" tickLine={false} axisLine={false} tick={{ fill: "#a9a1a0", fontSize: 11 }}/><YAxis tickLine={false} axisLine={false} tick={{ fill: "#a9a1a0", fontSize: 11 }}/><Tooltip cursor={{ fill: "rgba(255,255,255,.04)" }} contentStyle={{ background: "#1e1b1a", border: "1px solid #49423c", borderRadius: 8, color: "#fff" }}/><Bar dataKey="receita" fill="#741d2a" radius={[3,3,0,0]}/><Bar dataKey="gastos" fill="#928681" radius={[3,3,0,0]}/><Bar dataKey="lucro" fill="#c8a057" radius={[3,3,0,0]}/></BarChart></ResponsiveContainer></div>; }
