"use client";

import { Bell, CalendarDays, Menu, Search } from "lucide-react";
import { useState } from "react";
import { Brand } from "./brand";

export function Topbar({ profile }: { profile?: { full_name?: string | null; role?: string | null } | null }) { const [period, setPeriod] = useState("Últimos 7 dias"); return <header className="sticky top-0 z-20 flex h-16 items-center gap-3 border-b border-[var(--line)] bg-[oklch(0.105_0.008_20_/_0.94)] px-4 backdrop-blur lg:px-7"><div className="lg:hidden"><Menu size={20} /></div><div className="lg:hidden"><Brand compact /></div><div className="relative hidden max-w-[410px] flex-1 md:block"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-[var(--muted)]"/><input aria-label="Busca global" className="input pl-9" placeholder="Buscar páginas, vídeos, UTMs e produtos…" /></div><div className="ml-auto flex items-center gap-2"><label className="hidden items-center gap-2 rounded-lg border border-[var(--line)] bg-[var(--surface)] px-2.5 py-2 text-xs text-[var(--muted)] sm:flex"><CalendarDays size={15}/><select aria-label="Período" value={period} onChange={(e) => setPeriod(e.target.value)} className="bg-transparent text-xs text-white outline-none"><option>Hoje</option><option>Ontem</option><option>Últimos 7 dias</option><option>Mês atual</option><option>Personalizado</option></select></label><button aria-label="Notificações" className="btn !p-2"><Bell size={16}/></button><button className="flex items-center gap-2 rounded-lg px-2 py-1.5 text-left hover:bg-[var(--surface)]"><span className="grid h-8 w-8 place-items-center rounded-full bg-[oklch(0.55_0.18_20)] text-xs font-bold text-white">{profile?.full_name?.slice(0, 2).toUpperCase() || "VH"}</span><span className="hidden sm:block"><span className="block max-w-28 truncate text-xs font-semibold text-white">{profile?.full_name || "Usuário Volkov"}</span><span className="block text-[10px] capitalize text-[var(--muted)]">{profile?.role || "operador"}</span></span></button></div></header>; }
